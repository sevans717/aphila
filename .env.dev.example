# Development environment overrides for mobile/frontend local testing
# Copy to .env.dev and update values as needed. Do NOT commit secrets.

REACT_NATIVE_API_URL=http://localhost:4000
USE_PGBOUNCER=false
DISABLE_PAYMENTS=true
