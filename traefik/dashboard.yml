http:
  routers:
    traefik-dashboard:
      rule: Host(`traefik.aphila.local`)
      entryPoints:
        - websecure
      service: api@internal
      middlewares:
        - traefik-auth

  middlewares:
    traefik-auth:
      basicAuth:
        usersFile: /etc/traefik/htpasswd
